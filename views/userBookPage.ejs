<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Travel - TravelKuy</title>
    <style>
        .booking-card {
            max-width: 800px;
            margin: 2rem auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .card-img-top {
            width: 100%;
            height: 400px;
            object-fit: cover;
            transition: all 0.3s ease;
        }
        .card-img-top:hover {
            transform: scale(1.05);
        }
        .card-body {
            padding: 2rem;
        }
        .card-title {
            font-size: 1.75rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #333;
        }
        .card-subtitle {
            font-size: 1.1rem;
            color: #666;
            margin-bottom: 0.5rem;
        }
        .price {
            font-size: 1.5rem;
            color: #D32F2F;
            font-weight: 600;
            margin: 1rem 0;
        }
        .form-control {
            border-radius: 8px;
            padding: 0.75rem 1rem;
            border: 1px solid #ddd;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: #D32F2F;
            box-shadow: 0 0 0 0.2rem rgba(211, 47, 47, 0.25);
        }
        .btn-success {
            background: #D32F2F;
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 25px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-success:hover {
            background: #B71C1C;
            transform: translateY(-2px);
        }
        .error-message {
            color: #D32F2F;
            font-size: 0.875rem;
            margin-top: 0.5rem;
            animation: fadeIn 0.3s ease;
        }
        .card-footer {
            background: #f8f9fa;
            padding: 1rem 2rem;
            color: #666;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <%- include('_nav') %>

    <div class="container">
        <div class="booking-card">
            <img src="<%= dataTravelCategoryById.imgUrl %>" class="card-img-top" alt="<%= dataTravelCategoryById.nameTravel %>">
            <div class="card-body">
                <h2 class="card-title"><%= dataTravelCategoryById.airlineName %></h2>
                <h6 class="card-subtitle">Schedule: <%= dataTravelCategoryById.formatDate(dataTravelCategoryById.schedule).resShow %></h6>
                <h6 class="card-subtitle">Category: <%= dataTravelCategoryById.Category.nameCategory %></h6>
                <div class="price">Price: <%= dataTravelCategoryById.Category.newPrice %></div>
                <p class="card-text"><%= dataTravelCategoryById.Category.description %></p>
                <p class="card-subtitle">Capacity Available: <%= dataTravelCategoryById.capacity %></p>

                <form method="post" action="" class="mt-4">
                    <div class="row align-items-end">
                        <div class="col-md-8 mb-3">
                            <label class="form-label">Quantity</label>
                            <input type="number" class="form-control" placeholder="Enter quantity" name="quantityOrder" min="1">
                        </div>
                        <div class="col-md-4 mb-3">
                            <button class="btn btn-success w-100">Book Now</button>
                        </div>
                    </div>
                    <% if (error) { %>
                        <div class="error-message"><%= error %></div>
                    <% } %>
                </form>
            </div>
            
            <div class="card-footer">
                <small>Last updated: <%= dataTravelCategoryById.formatDate(dataTravelCategoryById.updatedAt).resShow %></small>
            </div>
        </div>
    </div>

    <%- include('_bootstrap') %>
</body>
</html>